# =============================================================================
# UNIBOS-HUB Repository .gitignore TEMPLATE (KAYNAK ŞABLON)
# Hub server environment - Rocksteady + Bebop HA
# =============================================================================
# ⚠️  BU DOSYA KAYNAK TEMPLATE'DIR
#
# Bu template .gitignore dosyasına kopyalanarak kullanılır:
#   cp .gitignore.hub .gitignore
#
# TEMPLATE ÇALIŞMA MANTIĞI (5-Repo Architecture):
# - .gitignore.dev     → unibos-dev repo için
# - .gitignore.manager → unibos-manager repo için
# - .gitignore.hub     → unibos-hub repo için (bu dosya)
# - .gitignore.node    → unibos (node) repo için
# - .gitignore.worker  → unibos-worker repo için
#
# push-all komutu her repo'ya push ederken doğru template'i otomatik kopyalar:
#   dev     → cp .gitignore.dev .gitignore     → push to dev
#   manager → cp .gitignore.manager .gitignore → push to manager
#   hub     → cp .gitignore.hub .gitignore     → push to hub
#   node    → cp .gitignore.node .gitignore    → push to node
#   worker  → cp .gitignore.worker .gitignore  → push to worker
#
# DEĞİŞİKLİK YAPARKEN:
# ✅ Bu template'i (.gitignore.hub) düzenleyebilirsiniz
# ✅ Ortak değişiklikleri diğer template'lere de ekleyin
#
# Bu şablon unibos-hub repo'su için tasarlanmıştır.
# Merkezi hub server (rocksteady + bebop) için.
#
# Repo: https://github.com/unibosoft/unibos-hub
# Purpose: Hub server deployment (identity provider, registry, sync)
# Includes: hub profile, node profile, worker profile, all modules (backend)
# Excludes: dev profile, manager profile (GÜVENLİK), archive/, data/
# =============================================================================

# ===== CRITICAL: EXCLUDE DEV & MANAGER PROFILES (SECURITY) =====
# Development and manager profiles should NEVER be on hub server
core/profiles/dev/
core/profiles/manager/
setup-dev.py
setup-manager.py

# Deploy system is dev-only
deploy/

# Hub deployment configs (contain secrets)
*.config.json

# ===== CRITICAL: NO ARCHIVES ON HUB =====
# Archives are development-only, never on production
archive/
archive_backup*/

# ===== CRITICAL: NO RUNTIME DATA IN REPO =====
# All data is managed separately on server
data/
data_db/
data_db_backup*/

# Build artifacts - never on server
/build/
/dist/

# ===== EXCLUDE DEV-ONLY FILES =====
# Claude and development artifacts
.claude/
CLAUDE_COMMUNICATION_LOG_*.md
Screenshot*.png
*.screenshot.png
screenshots_archive/
quarantine/
berk_claude_file_pool_DONT_DELETE/

# ===== EXCLUDE DEV-ONLY DOCS =====
TODO.md
RULES.md
docs/rules/
docs/design/decisions/

# ===== PYTHON ENVIRONMENT =====
venv/
.venv/
env/
.env/
ENV/

# ===== PYTHON CACHE & BUILD =====
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg

# ===== DATABASE FILES =====
*.sqlite3
*.sqlite3-journal
*.sqlite3.backup
*.db
*.db.backup
db.sqlite3*

# ===== LOG FILES =====
*.log
*.log.*
logs/
core/clients/web/logs/

# ===== DJANGO GENERATED =====
staticfiles/
core/clients/web/staticfiles/
core/clients/web/media/
modules/*/backend/staticfiles/
modules/*/backend/media/
modules/*/backend/uploads/

# ===== ENVIRONMENT & SECRETS =====
.env
.env.*
!.env.example
*.key
*.pem
*.cert
*.crt
.api_keys.json
secrets/
credentials/

# ===== SYSTEM FILES =====
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
*~

# ===== PID FILES =====
*.pid
.*.pid

# ===== TEMPORARY FILES =====
*.tmp
*.temp
*.bak
*.backup
tmp/
temp/

# ===== BACKUP DIRECTORIES =====
backup/
backups/

# ===== NODE.JS =====
node_modules/
npm-debug.log
yarn-error.log

# ===== FLUTTER/MOBILE - EXCLUDE ENTIRELY =====
# Mobile app is development-only, not needed on hub
core/clients/mobile/
modules/*/mobile/

# ===== TEST FILES =====
.pytest_cache/
.coverage
htmlcov/
*.test.html
*_test.html
test_*.html
test_*.py
*_test.py

# ===== LARGE MEDIA =====
*.mp4
*.avi
*.mov
*.zip
*.tar.gz
*.rar

# ===== SQL BACKUPS =====
*.sql
*.sql.gz

# ===== TEMPORARY DATA =====
all_versions.txt
missing_versions.txt
pushed_versions.txt

# ===== EXCLUDE DEV-ONLY SETTINGS =====
# Keep: hub.py, node.py, worker.py, base.py
# Exclude: development.py
core/clients/web/unibos_backend/settings/development.py

# ===== IGNORE FILES THEMSELVES (META) =====
# NOTE: .gitignore.* templates SHOULD be in dev repo only
# Hub repo only needs active .gitignore
.gitignore.dev
.gitignore.manager
.gitignore.hub
.gitignore.node
.gitignore.worker

# Legacy ignore files
.archiveignore
.rsyncignore
.prodignore
